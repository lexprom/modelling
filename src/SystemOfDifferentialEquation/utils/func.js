export function FunctionX (x, y, K) {
  return (-K * x)/ Math.sqrt(Math.pow(x, 2) + Math.pow(y,2))
}

export function FunctionY (x, y, K) {
  return (-K * y)/ Math.sqrt(Math.pow(x,2) + Math.pow(y,2))
}